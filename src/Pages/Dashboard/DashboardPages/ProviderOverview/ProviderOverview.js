import { Typography } from '@mui/material';
import { Box } from '@mui/system';
import React from 'react';


const ProviderOverview = () => {
    return (
        <Box>
           <Typography variant='h3'>ProviderOverview</Typography>
        </Box>
    );
};

export default ProviderOverview;