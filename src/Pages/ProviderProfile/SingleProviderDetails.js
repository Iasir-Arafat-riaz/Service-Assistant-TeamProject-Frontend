import { Typography } from '@mui/material';
import React from 'react';

const SingleProviderDetails = () => {
    return (
        <div>
            <Typography>This is a single provider profile</Typography>
        </div>
    );
};

export default SingleProviderDetails;